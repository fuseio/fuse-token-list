{
  "name": "fuse-default-token-list",
  "version": "1.5.5",
  "description": "◦ The Fuse default token list",
  "main": "build/fuse-default.tokenlist.json",
  "scripts": {
    "test": "mocha",
    "build": "rimraf build && mkdir -p build && node src/write.js > build/fuse-default.tokenlist.json",
    "build-local": "node src/write.js > fuse.tokenlist.json",
    "prepublishOnly": "npm test && npm run build"
  },
  "files": [
    "build/fuse-default.tokenlist.json"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fuseio/fuse-token-list.git"
  },
  "keywords": [
    "fuse",
    "default",
    "token",
    "list"
  ],
  "author": "Moody Salem",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/fuseio/fuse-token-list/issues"
  },
  "homepage": "https://github.com/fuseio/fuse-token-list#readme",
  "devDependencies": {
    "@ethersproject/address": "^5.0.2",
    "@uniswap/token-lists": "^1.0.0-beta.8",
    "ajv": "^6.12.3",
    "chai": "^4.2.0",
    "mocha": "^8.0.1",
    "rimraf": "^3.0.2"
  }
}
